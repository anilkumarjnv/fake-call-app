import 'package:flutter/material.dart';

class ActiveCallPage extends StatelessWidget {
  const ActiveCallPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Active Call Page'),
      ),
    );
  }
}
